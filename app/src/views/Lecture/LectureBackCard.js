import React from 'react'
import Typography from '@material-ui/core/Typography'

const AssignmentBackCard = ({ description }) => {
  return <Typography component='p'>
    {description}
  </Typography>
}

export default AssignmentBackCard
